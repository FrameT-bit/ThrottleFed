{
  "format": 1,
  "updated": "2026-09-25",
  "note": "The index the store reads. 'package' is relative to this directory, and 'requires' is what has to exist on the machine for the plugin to have anything to drive. Adding an entry for a plugin hosted elsewhere is a matter of pointing 'package' at a package that ships with it, or extending this format with a download instead of a directory.",
  "plugins": [
    {
      "id": "dell-pm-va",
      "name": "Dell Power Manager",
      "vendor": "Dell",
      "tab": "Dell PM",
      "summary": "Thermal mode and battery charging from the Dell firmware attributes.",
      "detail": "Dell keeps part of the thermal and power policy in BIOS attributes instead of registers. This is the same channel Dell Power Manager writes through, reached directly instead of through Dell Command | Configure (cctk), which needs root and can also change Secure Boot, TPM and boot order. Firmware state, survives a reboot, and on some models a write only applies on the next boot.",
      "upstream": "https://github.com/alexVinarskis/dell-powermanager",
      "license": "GPL-3.0 (this plugin is original code; the upstream app is the reference for which attributes matter)",
      "requires": [
        "/sys/class/firmware-attributes/dell-wmi-sysman/attributes"
      ],
      "vendor_match": [
        "dell"
      ],
      "package": "packages/dell-pm-va",
      "credits": [
        {
          "name": "Dell Power Manager (cross-platform re-implementation)",
          "creator": "alexVinarskis (GitHub)",
          "repo": "https://github.com/alexVinarskis/dell-powermanager",
          "license": "GPL-3.0",
          "reuse": "which firmware attributes carry the thermal policy and the battery charge policy, and what each value means on the machine",
          "note": "This plugin is not a wrapper around it and no code was copied. It is written against the sysfs channel ThrottleFed already writes to, so it needs no new privilege and no root."
        },
        {
          "name": "Dell Command | Configure (formerly CCTK)",
          "creator": "Dell Inc.",
          "repo": "https://www.dell.com/support/kbdoc/en-us/000178000/dell-command-configure",
          "license": "proprietary, Dell's own",
          "reuse": "only the knowledge of which attributes exist and which values the firmware accepts",
          "note": "cctk reaches the same attributes but needs root to run, and it can also change Secure Boot, TPM and boot order. ThrottleFed stays on the narrow channel instead."
        }
      ]
    },
    {
      "id": "asus-armoury",
      "name": "ASUS ROG tunables",
      "vendor": "ASUS",
      "tab": "Asus",
      "summary": "Package power limits, GPU power and the firmware switches, from the ASUS firmware attributes.",
      "detail": "ASUS keeps the package power limits, the Nvidia power and the panel state in BIOS attributes instead of registers: the same values Armoury Crate drives on Windows. This is the channel the mainline asus-armoury driver publishes, so it needs no vendor tool, no daemon and no root beyond the privileged helper. The limits are kept per power source (AC and battery separately), the kernel publishes no bounds for them, and on some models every limit write is refused until a custom fan curve is active.",
      "upstream": "https://github.com/OpenGamingCollective/asusctl",
      "license": "GPL-3.0 (this plugin is original code; the upstream app and the kernel driver are the references for which attributes matter)",
      "requires": [
        "/sys/class/firmware-attributes/asus-armoury/attributes"
      ],
      "vendor_match": [
        "asus"
      ],
      "package": "packages/asus-armoury",
      "credits": [
        {
          "name": "asusctl (asusd, rog-control-center)",
          "creator": "Luke Jones and the asus-linux project, now the Open Gaming Collective",
          "repo": "https://github.com/OpenGamingCollective/asusctl",
          "license": "MPL-2.0",
          "reuse": "which firmware knobs matter on a ROG machine and what each one is for: the power limits, the GPU power, the panel modes, the POST sound",
          "note": "This plugin is not a wrapper around it and no code was copied. asusctl is a daemon with a D-Bus client; this plugin writes the attributes the kernel publishes, so none of it has to be installed."
        },
        {
          "name": "asus-armoury (the Linux kernel driver)",
          "creator": "Luke Jones, in the mainline kernel",
          "repo": "https://github.com/torvalds/linux/blob/master/drivers/platform/x86/asus-armoury.c",
          "license": "GPL-2.0-or-later",
          "reuse": "the attribute names, what each one means, and the fact that the driver checks a value against the limits it carries before the firmware sees it",
          "note": "Without this driver the attributes do not exist, and this plugin reports nothing rather than guessing. The LED and fan curve surfaces are not part of it."
        },
        {
          "name": "ASUS Armoury Crate",
          "creator": "ASUSTeK Computer Inc.",
          "repo": "https://rog.asus.com/content/armoury-crate/",
          "license": "proprietary, ASUS's own, Windows only",
          "reuse": "only the knowledge that these knobs exist in the firmware and what they are called. The values are the firmware's, not the application's",
          "note": "Nothing from it is used, packaged or required here, and it is not the reason the knobs work: the kernel driver is."
        }
      ]
    }
  ]
}
